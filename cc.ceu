println(...)

val f-name = ...[1]
val f-hand = `:pointer fopen($f-name.Dyn->Ncast.Vector.buf, "r")`
defer {
    `fclose($f-hand.Pointer);`
}

^["char.ceu"]
^["file.ceu"]   ;;(f-hand)
^["tokens.ceu"]
^["lexer.ceu"]

loop in iter(co-token) {
    println(tags(it), it)
}
